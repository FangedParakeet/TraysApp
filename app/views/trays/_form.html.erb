<%= form_for(@tray) do |f| %>
  <% if @tray.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tray.errors.count, "error") %> prohibited this tray from being saved:</h2>

      <ul>
      <% @tray.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.file_field :image %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
